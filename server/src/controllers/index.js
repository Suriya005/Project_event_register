const users = require('./users')
const pg_users = require('./pguser')
const reg_event = require('./reg_event')
const user_reg = require('./user_reg')


module.exports = {
  users,
  pg_users,
  reg_event,
  user_reg,
};